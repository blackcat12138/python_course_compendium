一、元组的创建与删除、访问、修改操作
    元组：元组与列表类似,元组是不可变的序列,使用一对'()'括号,相邻元素以','符号相隔,
         存放任意类型,不限制大小。
    (a).使用赋值运算符直接创建元组
    (b).创建空元组
    (c).创建数值元组,使用tuple()函数转换成元组类型。
    (d).del语句删除元素。
    (e).通过索引或切片访问元组中的元素。
    (f).由于元组不可变,就不能对单个元素值进行修改,只能对元组重新赋值。
二、元组推导式
    元组推导式与列表推导式用法类型
    randomnum=(random.randint(10,100) for i in range(10))
    print(tuple(rnadomnum))
三、列表与元组的区别
    (a).列表属于可变序列,可以修改或删除;元组属于不可变序列,元素不可修改。
    (b).列表可以使用append()、remove()、pop()等方法实现添加和修改列表元素;
        元组不支持这些方法,所以不能添加和修改、删除元素。
    (c).列表可以使用切片访问和修改列表中的元素;列表也支持切片但不执行修改。
    (d).元组比列表的访问和处理速度快,当需访问元素可以使用元组。
    (e).列表不能作为字典的键,元组可以。

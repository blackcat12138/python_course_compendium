一、python中的集合概述
    集合：用于保存不重复的元素,分为可变集合(set)和不可变集合(frozenset)
        可变集合是一种无序可变序列,集合元素放在'{}'括号中,相邻元素','符号分隔。
    1.集合创建形式
        (a).使用'{}'括号直接创建,语法格式如下：setname={ele 1,....,ele n}
            在创建集合中若输入重复元素,会只保留一个元素。
        (b).使用set()函数创建,语法格式如下：setname=set(iteration)
            set()：将序列转换为集合。
二、集合的基本操作
    由于set集合是可变集合,所以在创建集合后,可以对其添加和删除。
    1.集合添加
        使用add()函数实现向集合中添加元素。
    2.集合删除
        del语句删除整个集合。
            pop()：删除一个元素,并返回这个元素。
         remove()：删除集合中的元素,若不存在则报错。
        discard()：删除集合中的元素,不存在报错。
          clear()：清空集合中的所有元素。
    3.集合中的计算
        交集运算使用'&'符号
        并集运算使用'|'符号
        差集运算使用'-'符号
        对称差集运算使用'^'符号
三、总结
    列表(list)：可变可重复有序的序列,定义符号[]
    元组(tuple)：不可变可重复有序的序列,定义符号()
    字典(dictionary)：可变可重复无序的序列,定义符号{key:value}
    集合(set)：可变不可重复无序的序列,定义符号{}